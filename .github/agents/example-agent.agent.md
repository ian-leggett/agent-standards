---
name: example-agent
description: "Placeholder example custom agent. Replace this description with a specific trigger condition so it's clear when to pick this agent."
tools: ["search"]
model: "GPT-5.2"
---

# Example agent (placeholder)

This is an example GitHub Copilot custom agent showing the shape of a
project-level agent definition — replace these instructions with real,
specific instructions for what this agent should do.

## Responsibilities

1. State the one job this agent exists to do.
2. List the steps it follows to do that job.
3. State what it returns when it's done.

## Out of scope

- Anything outside this agent's one job — keep a custom agent narrow rather
  than a second general-purpose agent.
